# copy/paste resp. to/from X clipboard
set selection-clipboard clipboard

# invert colors
set recolor "true"
set recolor-keephue "true"

# synctex
set synctex "true"
set synctex-editor-command "/usr/bin/vim --servername vim --remote +%{line} %{input}"
